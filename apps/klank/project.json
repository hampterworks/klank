{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "klank",
  "projectType": "application",
  "targets": {
    "tauri": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tauri dev",
        "cwd": "apps/klank"
      }
    },
    "tauri:dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tauri dev",
        "cwd": "apps/klank"
      }
    },
    "tauri:build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tauri build",
        "cwd": "apps/klank"
      }
    }
  }
}
